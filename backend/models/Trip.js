import SavedTrip from './SavedTrip.js';

/**
 * Phase 0 Schema Duplication Reconciliation:
 * The canonical trip entity is SavedTrip.js.
 * Trip.js is maintained as an alias to SavedTrip for backwards compatibility.
 */
export default SavedTrip;
